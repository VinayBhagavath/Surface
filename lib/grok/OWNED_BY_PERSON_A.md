# OWNED BY PERSON A — `/lib/grok`

Grok reasoning calls (narration + synthesis prompts) for the evidence pipeline.
Part of the **research engine**.

**Person B (frontend / UI / voice) must NOT create or edit files here.**

Note: Person B's **voice** code lives in `/lib/voice`, NOT here. Voice is the
speech layer on top of narrations; the narration *text* is produced by Person A
in this directory. If the voice follow-up answer call reuses Person A's
narration prompt, coordinate — don't duplicate it here.

See `/CLAUDE.md` → Merge discipline.
