# OWNED BY PERSON A — `/lib/reference`

Reference data / lookup tables used by the research engine (e.g. gene→prior
tables, HPO mappings, ortholog references).

**Person B (frontend / UI / voice) must NOT create or edit files here.**

If the intake form needs the clinical-context → HPO label strings, those are
agreed as plain strings in the UI and mapped by Person A on their side — confirm
the exact label strings with Person A; do not implement the mapping here.

See `/CLAUDE.md` → Merge discipline.
